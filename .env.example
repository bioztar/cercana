# Copy to .env (gitignored). Both values are public by design (anon key) but still live only in env.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional: 1 = in-memory demo fixtures, no Supabase needed (offline demos, screenshots). Never in production.
# EXPO_PUBLIC_DEMO=1
