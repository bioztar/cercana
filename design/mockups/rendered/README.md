# rendered/

PNG renders of every `*.dc.html` screen (390 px wide; `Main` is the full 2500 px feed scroll), made with headless Chrome and an empty `support.js`, so anyone can view them without the Claude Design canvas. Some canvas variables still show as `{{ name }}` — those are placeholders, not copy.
